# Write your solution here
def is_leaf(heap, index): 
        leaf_index = heap[-1]
        if index == 0 and len(heap) > 0:
                leaf_index[0]
                return False
        else:   
                leaf_index
                return True

    